# OAuth 2.0 Client (Web) — Client ID. Sau khi đổi domain: Google Cloud Console → APIs & Services →
# Credentials → OAuth client → Authorized JavaScript origins: thêm https://domain-cua-ban (không / cuối),
# và http://localhost:5173 cho Vite dev. Rebuild client sau khi đổi biến này.
VITE_CLIENT_GG_ID=
VITE_SERVER_URL=http://localhost:5001/api/v1
VITE_DROPBOX_API=
VITE_EXCHANGERATE_API=
VITE_PAYPAL_CLIENT_ID=
VITE_PRICE_OF_POST_PER_DAY=